import React from "react";
import CreatePost from "./CreatePost";

const Form = () => {
  return (
    <div class="main-content">
      <section class="section">
        <CreatePost />
      </section>
    </div>
  );
};

export default Form;
